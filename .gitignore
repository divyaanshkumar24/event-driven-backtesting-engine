.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# local data cache
data_cache/
*.duckdb

# bundle outputs (generated artifacts, not source)
bundles/*
!bundles/.gitkeep

# viewer (Next.js)
viewer/node_modules/
viewer/.next/
viewer/out/
viewer/next-env.d.ts
viewer/.vercel/
# viewer/public/data/current/ is derived output (see engine/bundle/viewer_export.py),
# but it's committed intentionally: it's the demo data the deployed viewer shows.
# Regenerate with scripts/run_reference_backtest.py before redeploying to update it.

.DS_Store
